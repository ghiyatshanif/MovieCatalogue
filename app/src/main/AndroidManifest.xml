<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ghiyatshanif.moviecatalogue">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MainApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MovieCatalogue">
        <activity android:name=".presentation.splash.SplashActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.detail.TvShowDetailActivity"
            android:label="@string/label_tv_show_detail"
            android:screenOrientation="portrait" />
        <activity
            android:name=".presentation.detail.MovieDetailActivity"
            android:label="@string/label_movie_detail"
            android:screenOrientation="portrait" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>